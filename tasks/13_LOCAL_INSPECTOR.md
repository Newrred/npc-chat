# Task13 — Local live model inspector
User requested a separate live model testing window. Capture exact OpenAI-compatible request messages/schema/sampling per stage and attempt before sending, output and timing, final success/failure separately from durable commit. Show selected vs currently stored memory and derived profile/events, room/turn selection, live polling and historical trace browsing. Default OFF; explicit NPC_DEBUG_TRACE enables private sidecar tracing, bounded to 100 turns/1 hour, no credentials in trace, never public route or browser response. Keep existing local admin Host/Origin/method boundaries. No model behavior changes. Tests: capture correctness, failures/retries, disabled mode, retention/reset, admin boundary, UI safe rendering. Activate opt-in for user testing and open inspector. Document historical requests cannot be reconstructed exactly before tracing.

Completed: 545 tests, Ruff/compileall/JS syntax pass. Real public two-stage turn trace verified locally and synthetic reset verified. UI loaded and memory tab checked. Capture enabled in local private test env, inspector opened. See docs/LOCAL_INSPECTOR.md.
