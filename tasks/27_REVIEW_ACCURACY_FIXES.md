# Task 27 — 외부 검토 정확성 회귀 수정

## 목표

2026-09-21 GPT Pro 검토에서 지적되고 현재 코드로 독립 재현된 두 정확성 문제를 기존 구조와 모델 호출 수를 유지하며 수정한다.

## 기준선

- 시작 HEAD: `main@f368d45`
- 작업 전 관련 테스트: 63 passed
- 전체 직전 기준: 606 passed
- 서버 재기동 확인: 모델·웹·검사창·Quick Tunnel 정상

## 범위

- 취소 판정과 활성 화제 경계가 같은 helper를 사용하도록 통합
- 취소 부정·질문·인용은 기존 약속 화제를 지우지 않음
- 실제 취소 뒤 같은 발화의 새 제안은 새 사건으로 보존
- 근거 있는 취향 회상 문장을 캐릭터 설정의 말투로 표현
- 유이 반말과 띳띠 해요체를 유지하면서 원문 근거·80자 제한·metadata 일치를 보존
- 관련 회귀 테스트와 현재 상태 문서 갱신

## 제외

- 모델·프롬프트 구조 변경
- 추가 LLM 호출
- metadata 문맥 경량화와 성능 계측
- DB schema migration
- 의미 검색 도입

## 완료 조건

- 취소/취소 부정/질문/인용/취소 뒤 새 제안/여러 약속 중 하나 취소 회귀가 통과한다.
- 유이와 띳띠의 취향 회상 문장이 각 캐릭터 말투를 지킨다.
- 확정된 회상 대사와 metadata가 분석한 대사가 일치한다.
- 기존 전체 Python·프런트 테스트, lint, compile, 정적 검사가 통과한다.

## 완료 결과

- 새 회귀를 먼저 추가해 취소 부정·인용 3건, 취소 뒤 새 제안 1건, 띳띠 회상 말투 2건의 예상 실패를 확인했다.
- 취소 저장과 활성 화제 경계가 공통 판정을 사용한다. 실제 취소 발화의 경계 앞부분만 취소 근거로 사용하고, 같은 발화의 뒤쪽 새 제안은 별도 사건으로 보존한다.
- 캐릭터 설정에 `grounded_recall_template`을 추가했다. 원문 근거 선택과 80자 제한은 공통 로직에 남기고 유이는 반말, 띳띠는 해요체로 표현한다.
- 관련 70 tests, 전체 613 tests, 프런트·검사창 28 tests, Ruff, compileall, JavaScript 구문 및 diff 검사가 통과했다.
- 저장하지 않는 실제 9B 두 단계 호출에서 유이 `네가 '나는 커피를 싫어해'라고 했어.`, 띳띠 `전에 '나는 커피를 싫어해'라고 했어요.`를 확인했다. 두 요청 모두 reply/metadata 단계가 성공했고 각각 약 4.3초/4.0초였다.
- DB schema, 모델 호출 수, API와 기존 저장 데이터는 변경하지 않았다.
