# Task 26 — 카르티시아 다중 캐릭터 대화

> 2026-09-11 이름 설정: 본명은 `카르티시아`, 더 자주 불리는 별명과 화면 표시 이름은 `띳띠`다. 저장 호환성을 위해 내부 ID와 이미지 경로의 `cartethyia`는 유지한다.

## 목표

기존 유이 동작과 데이터를 보존하면서 대화 목록에서 명조의 카르티시아를 선택해 별도 대화를 할 수 있게 한다.

## 기준선

- 시작 HEAD: `main@4180e24`
- 작업 전 `python -m pytest -q`: 599 passed
- 제공 이미지 최초 확인 경로 오타로 1회 실패 후 사용자가 지정한 Downloads 이미지 폴더 확인
- 제공 이미지: canonical face 태그 17개, 각 512×600 PNG

## 범위

- `character_id`를 선택적으로 받는 기존 세션·대화·이력·초기화 API
- 허용된 캐릭터 설정 파일만 로드하고 알 수 없는 ID는 정형 오류로 거절
- 동일 프로필 안에서 캐릭터별 세션·이력·관계·기억 분리
- 카르티시아 캐릭터 프롬프트와 17개 정적 표정 이미지
- 대화 목록의 카르티시아 방과 캐릭터별 화면/브라우저 세션 상태
- 기존 요청의 `character_id` 생략은 계속 `default`(유이)

## 제외

- 새 모델 프로세스나 추가 모델 호출
- DB schema migration
- 캐릭터별 ComfyUI 생성 워크플로
- 원작 전체 스토리를 재현하는 지식베이스

## 완료 조건

- 유이와 카르티시아가 같은 프로필에서 서로 다른 대화·관계·기억을 사용한다.
- 카르티시아 요청은 카르티시아 프롬프트로 생성된다.
- 두 캐릭터의 17개 표정에 디코딩 가능한 정적 PNG가 있다.
- 새로고침 후 선택한 캐릭터의 대화가 복원되고 다른 방 기록이 섞이지 않는다.
- 기존 API 클라이언트와 유이 방 동작이 유지된다.
- 관련 Python/프런트 테스트, lint, compile, 정적 검사와 smoke가 통과한다.

## 완료 결과

- 전체 Python: 606 passed
- 프런트: 23 passed
- Ruff, compileall, JavaScript 구문, diff 검사 통과
- 브라우저에서 대화 목록, 카르티시아 방, 512×600 얼굴 화면 확인
- 실제 9B 모델에서 카르티시아 자기소개·해요체·happy 표정·이력 저장 확인 후 합성 방 초기화
- 기존 DB schema와 유이 기록 보존, 추가 migration 없음
